<svg width="700" height="300" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <style>
      .survivor-text {
        font-family: 'Poppins', 'Arial Black', sans-serif;
        font-weight: 800;
        font-size: 120px;
        fill: #0A3D91;
        letter-spacing: -2px;
      }
      .oneshot-text {
        font-family: 'Poppins', 'Arial', sans-serif;
        font-weight: 300;
        font-size: 48px;
        fill: #4FC3F7;
        letter-spacing: 12px;
      }
    </style>
  </defs>

  <!-- SURVIVOR -->
  <text x="50%" y="130" text-anchor="middle" class="survivor-text">SURVIVOR</text>

  <!-- ONE SHOT -->
  <text x="50%" y="200" text-anchor="middle" class="oneshot-text">ONE SHOT</text>
</svg>

